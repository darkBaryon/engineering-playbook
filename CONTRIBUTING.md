# 维护规范

## 先判断改动属于哪一层

| 改动 | 应修改的位置 |
|---|---|
| 三套流程都必须遵守的原则 | `core/` |
| 只影响开发、重构或测试之一 | 对应 `profiles/<name>/` |
| 可复制的文档结构 | `templates/<name>/` |
| 字段、状态或档位枚举 | `schemas/` |
| 某项目的地址、分支、命令或设备限制 | 业务项目，不进入本仓 |

禁止为了减少一段重复，把只有表面相似、语义不同的规则强行放进 Core。

## 修改流程

1. 写明问题、影响的 Profile、预期行为和非目标；
2. 修改规范正文；涉及产物结构时同步模板和 Schema；
3. 检查内部链接、状态枚举、Gate 边界和术语一致性；
4. 用至少一个真实项目做纸面演练，但不把案例记录提交到本仓；
5. 由未参与起草的评审职责复核；
6. 按语义化版本更新 `VERSION` 和 `CHANGELOG.md`；
7. 发布标签后，由各项目自行升级固定版本。

## 兼容性

- 新增可选字段应有默认行为；
- 删除或重命名状态、Gate、必填章节属于破坏性变更；
- Profile 不得降低 Core 的独立复核、证据留存和偏差登记要求；
- 历史业务记录按当时固定版本解释，不追溯改写。

## 写作要求

- 流程按职责而不是具体人员或模型书写；
- 规则必须可执行、可验证，避免“适当优化”之类空话；
- 示例只用于解释规则，不承载项目事实；
- 不引用聊天记录作为唯一依据；
- 不在本仓写入账号、token、合同、客户数据或其他敏感信息。
