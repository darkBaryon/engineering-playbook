# 维护说明

本仓以“执行者容易读懂”为第一目标。三套规范分别自包含，允许为保持阅读连续而重复共同规则；不要为了减少几段文字，再建立读者必须先理解的公共流程层。

## 内容放在哪里

| 内容 | 位置 |
|---|---|
| 开发任务如何执行 | `development/` |
| 重构任务如何执行 | `refactoring/` |
| 测试任务如何执行 | `testing/` |
| 会被复制使用的记录骨架 | `templates/` |
| 项目地址、分支、命令、设备和资源限制 | 业务项目 |
| 真实需求、方案、评审和测试记录 | 业务项目 |

一个规则即使同时适用于三套规范，也应使用各自流程中的自然语言写回相应章节。这里通过一致性检查维护共同纪律，不通过正文引用实现去重。

## 共同规则一致性表

修改任一项时，检查三套规范中对应表达是否仍然一致：

| 规则 | 开发 | 重构 | 测试 |
|---|---|---|---|
| 固定精确基线 | 需求立案 | 现状盘点 | 测试准入 |
| 风险分级与升档 | 开发定档 | 重构定档 | 测试风险分级 |
| 职责与独立性 | 方案/代码复核 | 方案/代码复核 | 独立测试结论 |
| 偏差即停 | 实施阶段 | 迁移阶段 | 测试对象或条件变化 |
| 机械检查与事实表 | 开发预飞 | 重构预飞和终扫 | 自动化与运行态核对 |
| 历史不可覆盖 | 方案和评审轮次 | 蓝图、方案和评审轮次 | 测试与复测轮次 |
| 用户决策边界 | Gate 1/2 | Gate 1/2 | Gate T0/T1/T2 |
| 证据交接 | commit、diff、验收记录 | 基线、迁移和行为对账 | 版本、环境和原始结果 |

## 修改流程

1. 写清问题、影响的规范、预期行为和非目标。
2. 在受影响规范中按执行顺序修改正文；如果是共同规则，逐项检查另外两套规范。
3. 产物结构变化时同步 `templates/`。
4. 使用至少一个真实任务做纸面演练，但不把任务记录提交到本仓。
5. 由未参与起草的复核职责检查可执行性、链接、术语和遗漏。
6. 按版本规则更新 `VERSION` 和 `CHANGELOG.md`。
7. 发布标签后，由业务项目自行决定何时升级采用版本。

## 版本规则

- Patch：措辞、链接、错别字或不改变流程行为的说明修订；
- Minor：新增向后兼容的可选规则或模板；
- Major：改变强制 Gate、状态、职责边界、必需产物或目录契约。

历史记录始终按创建时采用的 Playbook 版本解释，不追溯改写。

## 写作要求

- 首页先给完整流程和最小动作，再解释原理；
- 章节按实际执行顺序排列，不按抽象概念横向切割；
- 写清谁做、做什么、产出什么、何时停止；
- 规则必须可执行、可验证，避免“适当处理”一类空话；
- 说明规则要防止的真实风险，但示例不承载项目事实；
- 流程按职责书写，不绑定具体人员、Agent 产品或模型；
- 不写账号、token、合同、客户数据或其他敏感信息。
