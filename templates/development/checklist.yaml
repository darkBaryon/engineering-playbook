schema_version: 1
profile: development
playbook_version: <version>
baseline: <完整commit>

commands:
  - name: <lint-or-analyze>
    run: [<argv>]
  - name: <tests>
    run: [<argv>]
  - name: <build-or-demo>
    run: [<argv>]

required_artifacts: []
required_symbols: []
forbidden_markers: []
generated_files_must_be_clean: []
